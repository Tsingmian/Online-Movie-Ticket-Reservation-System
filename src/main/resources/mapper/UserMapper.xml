<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.movie_ticket_reservation.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.example.movie_ticket_reservation.entity.User">
        SELECT id, username, password, role, status, create_time
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.example.movie_ticket_reservation.entity.User">
        INSERT INTO user(username, password, role, status)
        VALUES (#{username}, #{password}, #{role}, #{status})
    </insert>

</mapper>
